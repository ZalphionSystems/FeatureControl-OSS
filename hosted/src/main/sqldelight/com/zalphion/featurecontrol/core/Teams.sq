get:
SELECT * FROM core.teams WHERE team_id = ?;

getAll:
SELECT * FROM core.teams WHERE team_id IN ?;

upsert:
INSERT INTO core.teams
    (team_id, team_name)
    VALUES (:id, :name)
ON CONFLICT (team_id)
DO UPDATE SET
    team_name = :name;

delete:
DELETE FROM core.teams WHERE team_id = ?;